import { CoreEngine } from '../engine/CoreEngine';
import { getSerializedSceneData } from '../engine/ScenePersistence';
import { assetDB } from './AssetDatabase';

export class Exporter {
    public static async exportStandalone(engine: CoreEngine) {
        console.log("Starting export...");
        
        const sceneData = getSerializedSceneData(engine);
        const assets = await assetDB.getAllAssets();
        
        const parts: string[] = [];
        parts.push("<!DOCTYPE html><html lang=\"en\"><head><meta charset=\"UTF-8\">");
        parts.push("<meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\"><title>Axiom Engine Export</title>");
        parts.push("<style>body,html{margin:0;padding:0;width:100%;height:100%;overflow:hidden;background:#0b111a;color:#fff;font-family:sans-serif;}#canvas{width:100%;height:100%;outline:none;display:block;}#loading{position:fixed;top:0;left:0;width:100%;height:100%;display:flex;align-items:center;justify-content:center;background:#0b111a;z-index:100;flex-direction:column;gap:20px;}.spinner{width:50px;height:50px;border:3px solid rgba(75,142,184,0.1);border-top-color:#4b8eb8;border-radius:50%;animation:spin 1s linear infinite;}@keyframes spin{to{transform:rotate(360deg);}}</style>");
        parts.push("<script src=\"https://cdn.babylonjs.com/babylon.js\"></script>");
        parts.push("<script src=\"https://cdn.babylonjs.com/loaders/babylonjs.loaders.min.js\"></script>");
        parts.push("<script src=\"https://cdn.babylonjs.com/materialsLibrary/babylonjs.materials.min.js\"></script>");
        parts.push("<script src=\"https://cdn.babylonjs.com/proceduralTexturesLibrary/babylonjs.proceduralTextures.min.js\"></script>");
        parts.push("<script src=\"https://cdn.babylonjs.com/havok/HavokPhysics_umd.js\"></script>");
        parts.push("</head><body><div id=\"loading\"><div class=\"spinner\"></div><div style=\"letter-spacing: 2px; font-size: 14px; color: #4b8eb8;\">LOADING ASSETS...</div></div><canvas id=\"canvas\"></canvas><script>");
        parts.push("const { Engine, Scene, Vector3, UniversalCamera, HemisphericLight, DirectionalLight, PointLight, SpotLight, MeshBuilder, TransformNode, Mesh, Color3, Color4, StandardMaterial, ShadowGenerator, SceneLoader, SkyMaterial, GridMaterial, PhysicsAggregate, PhysicsShapeType, PhysicsMotionType, Quaternion, PhysicsShape, Matrix } = BABYLON;");
        
        parts.push("const SCENE_DATA = " + JSON.stringify(sceneData) + ";");
        parts.push("const ASSET_DATA = " + JSON.stringify(assets) + ";");

        parts.push("class StandaloneEngine {");
        parts.push("constructor(canvas) { this.babylonEngine = new Engine(canvas, true); this.babylonScene = new Scene(this.babylonEngine); this.entities = new Map(); this.babylonNodes = new Map(); this.shadowGenerators = new Map(); this.skyMaterial = null; this.assetPromises = []; this.babylonScene.imageProcessingConfiguration.toneMappingEnabled = true; this.babylonScene.imageProcessingConfiguration.toneMappingType = BABYLON.ImageProcessingConfiguration.TONEMAPPING_ACES; }");
        parts.push("async initPhysics() { try { const havok = await HavokPhysics(); const hk = new BABYLON.HavokPlugin(true, havok); this.babylonScene.enablePhysics(new Vector3(0, -9.81, 0), hk); } catch (e) { console.error(\"Physics fail\", e); } }");
        parts.push("async start() { for (const se of SCENE_DATA.entities) { this.entities.set(se.id, se); this.createEntity(se); } await Promise.all(this.assetPromises); const skyEntity = SCENE_DATA.entities.find(e => e.type === \"Sky\"); this.setupEnvironment(skyEntity); for (const se of SCENE_DATA.entities) { const node = this.babylonNodes.get(se.id); if (!node) continue; node.parent = se.parentId ? this.babylonNodes.get(se.parentId) : null; if (se.transform) { const t = se.transform; if (node.position) node.position.set(t.px, t.py, t.pz); if (node.rotationQuaternion !== undefined) { node.rotationQuaternion = Quaternion.RotationYawPitchRoll(t.ry, t.rx, t.rz); } else if (node.rotation) { node.rotation.set(t.rx, t.ry, t.rz); } if (node.scaling) node.scaling.set(t.sx, t.sy, t.sz); } if (node instanceof Mesh && se.castShadows) { for (const sg of this.shadowGenerators.values()) sg.addShadowCaster(node, true); } if (se.type === \"Sky\") this.applySkySettings(se); this.applyPhysics(se, node); } this.runtime = new StandaloneRuntime(this.babylonScene, this.entities, this.babylonNodes); this.runtime.start(); this.babylonEngine.runRenderLoop(() => { const delta = Math.min(this.babylonEngine.getDeltaTime() / 1000, 0.1); this.runtime.update(delta); this.babylonScene.render(); }); window.addEventListener(\"resize\", () => this.babylonEngine.resize()); document.getElementById(\"loading\").style.display = \"none\"; }");
        parts.push("setupEnvironment(e) { const hemi = new HemisphericLight(\"__editor_hemi__\", new Vector3(0, 1, 0), this.babylonScene); if (e) { this.babylonScene.ambientColor = Color3.FromHexString(e.ambientColor || \"#1b2635\"); this.babylonScene.environmentIntensity = e.environmentIntensity || 1.0; hemi.intensity = e.environmentIntensity || 1.0; hemi.diffuse = Color3.FromHexString(e.ambientColor || \"#1b2635\"); const grid = MeshBuilder.CreateGround(\"__grid__\", { width: 500, height: 500 }, this.babylonScene); const gridMat = new GridMaterial(\"__gridMat__\", this.babylonScene); gridMat.mainColor = new Color3(0.05, 0.06, 0.08); gridMat.lineColor = new Color3(0.2, 0.25, 0.35); grid.material = gridMat; grid.position.y = -0.01; grid.receiveShadows = true; grid.setEnabled(e.showGrid !== false); if (e.groundLevelEnabled && e.groundLevelCollidable) { const groundBox = MeshBuilder.CreateBox(\"__global_physics_ground__\", { width: 2000, height: 1, depth: 2000 }, this.babylonScene); groundBox.isVisible = false; groundBox.position.y = (e.groundLevel || 0) - 0.5; new PhysicsAggregate(groundBox, PhysicsShapeType.BOX, { mass: 0, friction: 0.5, restitution: 0.1 }, this.babylonScene); } } else { const sun = new DirectionalLight(\"__default_sun__\", new Vector3(-1, -2, -1), this.babylonScene); sun.intensity = 1.8; const sg = new ShadowGenerator(2048, sun); sg.useBlurExponentialShadowMap = true; this.shadowGenerators.set(\"__default_sun__\", sg); } }");
        parts.push("createEntity(e) { let node; if (e.type === \"Mesh\") { if (e.meshType === \"Cube\") node = MeshBuilder.CreateBox(e.name, {size:1}, this.babylonScene); else if (e.meshType === \"Sphere\") node = MeshBuilder.CreateSphere(e.name, {diameter:1}, this.babylonScene); else if (e.meshType === \"Plane\") node = MeshBuilder.CreateGround(e.name, {width:5, height:5}, this.babylonScene); else if (e.meshType === \"Cylinder\") node = MeshBuilder.CreateCylinder(e.name, {height:2, diameter:1}, this.babylonScene); else if (e.meshType === \"Capsule\") node = MeshBuilder.CreateCapsule(e.name, {height:2, radius:0.5}, this.babylonScene); else if (e.meshType === \"Torus\") node = MeshBuilder.CreateTorus(e.name, {diameter:1, thickness:0.35}, this.babylonScene); else if (e.meshType === \"Cone\") node = MeshBuilder.CreateCylinder(e.name, {height:2, diameterTop:0, diameterBottom:1}, this.babylonScene); else if (e.meshType === \"Disc\") { node = MeshBuilder.CreateDisc(e.name, {radius:1}, this.babylonScene); node.rotation.x = Math.PI/2; } else if (e.meshType === \"ImportedModel\") { node = new TransformNode(e.name, this.babylonScene); const asset = ASSET_DATA.find(a => a.id === e.modelAssetId); if (asset) { const p = SceneLoader.ImportMeshAsync(\"\", \"\", asset.data, this.babylonScene).then(res => { res.meshes.forEach(m => { if(!m.parent) m.parent = node; m.receiveShadows = !!e.receiveShadows; }); }); this.assetPromises.push(p); } } if (node instanceof Mesh) { const mat = new StandardMaterial(e.name + \"_mat\", this.babylonScene); mat.diffuseColor = Color3.FromHexString(e.materialColor || \"#ffffff\"); const met = e.materialMetallic || 0; mat.specularColor = new Color3(met, met, met); mat.roughness = e.materialRoughness || 0.7; if (e.emissiveEnabled) mat.emissiveColor = Color3.FromHexString(e.materialEmissive).scale(e.emissiveIntensity||1); node.material = mat; node.receiveShadows = !!e.receiveShadows; } } else if (e.type === \"Light\") { if (e.lightType === \"Directional\") { node = new DirectionalLight(e.name, new Vector3(e.lightDirection?.x||-1, e.lightDirection?.y||-2, e.lightDirection?.z||-1), this.babylonScene); if (e.lightShadowEnabled) { const sg = new ShadowGenerator(e.lightShadowMapSize || 1024, node); sg.useBlurExponentialShadowMap = true; sg.setDarkness(e.lightShadowDarkness || 0.3); this.shadowGenerators.set(e.id, sg); } } else if (e.lightType === \"Point\") node = new PointLight(e.name, Vector3.Zero(), this.babylonScene); else if (e.lightType === \"Spot\") node = new SpotLight(e.name, Vector3.Zero(), new Vector3(0,-1,0), Math.PI/3, 2, this.babylonScene); else node = new HemisphericLight(e.name, new Vector3(0,1,0), this.babylonScene); node.intensity = e.lightIntensity || 1.0; if (e.lightColor) node.diffuse = Color3.FromHexString(e.lightColor); } else if (e.type === \"Camera\") { node = new UniversalCamera(e.name, Vector3.Zero(), this.babylonScene); if (e.isMainCamera) this.babylonScene.activeCamera = node; } else if (e.type === \"Sky\") { node = MeshBuilder.CreateSphere(\"__sky__\", { diameter: 150000, segments: 16 }, this.babylonScene); node.infiniteDistance = true; this.skyMaterial = new SkyMaterial(\"__skyMat__\", this.babylonScene); this.skyMaterial.backFaceCulling = false; node.material = this.skyMaterial; } else node = new TransformNode(e.name, this.babylonScene); if (node) this.babylonNodes.set(e.id, node); }");
        parts.push("applySkySettings(e) { if (!this.skyMaterial) return; this.skyMaterial.turbidity = e.skyTurbidity || 2.0; this.skyMaterial.luminance = e.skyLuminance || 1.0; this.skyMaterial.inclination = e.skyInclination || 0.2; this.skyMaterial.azimuth = e.skyAzimuth || 0.15; }");
        parts.push("applyPhysics(e, node) { if (e.physicsType === \"None\" && !e.hasCollider) return; if (node.physicsBody) { node.physicsBody.dispose(); node.physicsBody = null; } const mass = e.physicsType === \"Dynamic\" ? (e.mass ?? 1) : 0; let motion = PhysicsMotionType.STATIC; if (e.physicsType === \"Dynamic\") motion = PhysicsMotionType.DYNAMIC; else if (e.physicsType === \"Kinematic\") motion = PhysicsMotionType.ANIMATED; let props = { mass, friction: e.friction ?? 0.5, restitution: e.restitution ?? 0.1 }; let shapeType = PhysicsShapeType.BOX; let mergedMesh = null; if (e.meshType === \"ImportedModel\") { const childMeshes = node.getChildMeshes(); if (childMeshes.length === 0) return; const meshesToMerge = []; childMeshes.forEach(m => { if (m instanceof Mesh && m.getTotalVertices() > 0) { const clone = m.clone(\"__temp_clone__\"); if (clone) { clone.setParent(null); clone.computeWorldMatrix(true); meshesToMerge.push(clone); } } }); if (meshesToMerge.length > 0) { try { mergedMesh = Mesh.MergeMeshes(meshesToMerge, true, true, undefined, false, true); if (mergedMesh) { mergedMesh.name = \"__physics_collider__\"; mergedMesh.setParent(node); mergedMesh.isVisible = false; mergedMesh.isPickable = false; shapeType = PhysicsShapeType.MESH; } } catch (err) { console.warn(\"Mesh merge failed, fallback to Box\", err); } } } else if (e.meshType === \"Sphere\") shapeType = PhysicsShapeType.SPHERE; else if (e.meshType === \"Capsule\" || e.meshType === \"Cylinder\") shapeType = PhysicsShapeType.CAPSULE; else if (e.meshType === \"Plane\") { shapeType = PhysicsShapeType.BOX; props.extents = new Vector3(5, 0.1, 5); } try { const physicsTarget = (e.meshType === \"ImportedModel\" && mergedMesh) ? mergedMesh : node; const agg = new PhysicsAggregate(physicsTarget, shapeType, props, this.babylonScene); node.physicsBody = agg.body; node.physicsBody.setMotionType(motion); if (e.lockRotationX || e.lockRotationY || e.lockRotationZ) { const mp = agg.body.getMassProperties(); if (e.lockRotationX) mp.inertia.x = 0; if (e.lockRotationY) mp.inertia.y = 0; if (e.lockRotationZ) mp.inertia.z = 0; agg.body.setMassProperties(mp); } if (e.collidable) { agg.shape.filterMembershipMask = e.collisionLayer ?? 1; agg.shape.filterCollideMask = e.collisionMask ?? 0xFFFFFFFF; } else { agg.shape.filterMembershipMask = 0; agg.shape.filterCollideMask = 0; } } catch(err) { console.warn(\"Physics fail\", e.name, err); } }");
        parts.push("}");
        
        parts.push("class StandaloneRuntime {");
        parts.push("constructor(scene, entities, nodes) { this.scene = scene; this.entities = entities; this.nodes = nodes; this.activeScripts = []; this.keys = {}; }");
        parts.push("start() { window.addEventListener(\"keydown\", e => this.keys[e.code] = true); window.addEventListener(\"keyup\", e => this.keys[e.code] = false); for (const entity of this.entities.values()) { if (entity.script && entity.script.trim() !== \"\") { try { const node = this.nodes.get(entity.id); if (!node) continue; const ctx = { node, entity, Vector3, Input: { is_action_pressed: (a) => { if (a === \"move_forward\" || a === \"ui_up\") return this.keys[\"KeyW\"] || this.keys[\"ArrowUp\"]; if (a === \"move_backward\" || a === \"ui_down\") return this.keys[\"KeyS\" ] || this.keys[\"ArrowDown\"]; if (a === \"move_left\" || a === \"ui_left\") return this.keys[\"KeyA\"] || this.keys[\"ArrowLeft\"]; if (a === \"move_right\" || a === \"ui_right\") return this.keys[\"KeyD\"] || this.keys[\"ArrowRight\"]; if (a === \"jump\") return this.keys[\"Space\"]; return this.keys[a] || false; }}, delta: 0, translate: (x, y, z) => { const v = new Vector3(x, y, z); const worldV = Vector3.TransformCoordinates(v, node.getWorldMatrix().getRotationMatrix()); if (node.physicsBody) { node.physicsBody.setTargetTransform(node.position.add(worldV), node.rotationQuaternion); } else if (node.position) node.position.addInPlace(worldV); }, rotate_y: (angle) => { if (node.rotation) { node.rotation.y += angle; if(node.physicsBody) node.physicsBody.setTargetTransform(node.position, node.rotationQuaternion); } }, set_linear_velocity: (x,y,z) => node.physicsBody?.setLinearVelocity(new Vector3(x,y,z)), get_linear_velocity: () => { const vel = node.physicsBody?.getLinearVelocity() || {x:0,y:0,z:0}; return {x: vel.x, y: vel.y, z: vel.z}; }, apply_impulse: (x,y,z) => { if (node.physicsBody) node.physicsBody.applyImpulse(new Vector3(x,y,z), node.getAbsolutePosition()); }, is_on_floor: () => Math.abs(node.physicsBody?.getLinearVelocity().y || 0) < 0.1, move_and_slide: (vx, vy, vz) => { if (node.physicsBody) { node.physicsBody.setLinearVelocity(new Vector3(vx, vy, vz)); return Math.abs(node.physicsBody.getLinearVelocity().y) < 0.1; } return false; }, get_node: (name) => { const e = Array.from(this.entities.values()).find(en => en.name === name); return e ? this.nodes.get(e.id) : null; } }; const func = new Function(\"ctx\", \"with(ctx) { \" + entity.script + \"\\n return { _ready: typeof _ready !== 'undefined' ? _ready : null, _process: typeof _process !== 'undefined' ? _process : null }; }\"); const hooks = func(ctx); if (hooks._ready) hooks._ready.call(ctx); this.activeScripts.push({ hooks, ctx }); } catch(e) { console.error(\"Script Error:\", entity.name, e); } } } }");
        parts.push("update(delta) { for (const s of this.activeScripts) { if (s.hooks._process) { s.ctx.delta = delta; try { s.hooks._process.call(s.ctx, delta); } catch(e) { console.error(e); } } } for (const entity of this.entities.values()) { if (entity.type === \"Camera\" && entity.cameraFollowTargetId) { const target = this.nodes.get(entity.cameraFollowTargetId); const cam = this.nodes.get(entity.id); if (target && cam && cam.position) { const off = entity.cameraOffset || {x:0,y:5,z:-10}; cam.position.copyFrom(target.position.add(new Vector3(off.x, off.y, off.z))); cam.setTarget(target.position); } } } }");
        parts.push("}");
        
        parts.push("const engine = new StandaloneEngine(document.getElementById(\"canvas\"));");
        parts.push("engine.initPhysics().then(() => engine.start());");
        parts.push("</script></body></html>");

        const finalHtml = parts.join('\n');
        const blob = new Blob([finalHtml], { type: 'text/html' });
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url;
        a.download = "AxiomGame.html";
        a.click();
        URL.revokeObjectURL(url);
        console.log("Export complete!");
    }
}
